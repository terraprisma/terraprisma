--- decompiled/TerrariaServerWindows/Terraria/GameContent/RGB/DeathShader.cs
+++ decompiled/TerrariaServerMac/Terraria/GameContent/RGB/DeathShader.cs
@@ -25,8 +_,8 @@
 	{
 		time *= 3f;
 		float amount = 0f;
-		float num = time % ((float)Math.PI * 4f);
+		float num = time % (MathF.PI * 4f);
-		if (num < (float)Math.PI)
+		if (num < MathF.PI)
 		{
 			amount = (float)Math.Sin(num);
 		}
